<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>dev.samypr100</groupId>
	<artifactId>aistool</artifactId>
	<packaging>jar</packaging>
	<version>${revision}</version>

	<name>${project.artifactId}-${project.version}</name>
	<description>
		AISTool provides a wrapper around aismessages — a light-weight and high-performing software library for decoding AIS messages related to maritime navigation and safety.
	</description>
	<url>https://github.com/samypr100/aistool</url>

	<developers>
		<developer>
			<name>samypr100</name>
		</developer>
	</developers>

	<licenses>
		<license>
			<name>Attribution-NonCommercial-ShareAlike 4.0 International (CC BY-NC-SA 4.0)</name>
			<url>https://creativecommons.org/licenses/by-nc-sa/4.0</url>
		</license>
	</licenses>

	<scm>
		<connection>scm:git:git://github.com/samypr100/aistool.git</connection>
		<developerConnection>scm:git:git@github.com:samypr100/aistool.git</developerConnection>
		<url>https://github.com/samypr100/aistool</url>
		<tag>HEAD</tag>
	</scm>

	<properties>
		<!-- https://maven.apache.org/maven-ci-friendly.html -->
		<revision>${maven.build.timestamp}</revision>
		<!-- Metadata Properties -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<license.licenceFile>LICENSE.txt</license.licenceFile>
		<mainClassName>dk.tbsalling.ais.cli.AisCli</mainClassName>
		<java.version>21</java.version>
		<maven.min.version>3.8.8</maven.min.version>
		<maven.compiler.source>${java.version}</maven.compiler.source>
		<maven.compiler.target>${java.version}</maven.compiler.target>
		<maven.compiler.release>${java.version}</maven.compiler.release>
		<maven.build.timestamp.format>yyyy.MM.dd</maven.build.timestamp.format>
		<!-- Plugin Properties -->
		<maven.compiler.plugin.version>3.14.0</maven.compiler.plugin.version>
		<maven.assembly.plugin.version>3.7.1</maven.assembly.plugin.version>
		<maven.enforcer.plugin.version>3.6.0</maven.enforcer.plugin.version>
		<maven.exec.plugin.version>3.5.1</maven.exec.plugin.version>
		<maven.gpg.plugin.version>3.2.8</maven.gpg.plugin.version>
		<maven.javadoc.plugin.version>3.11.2</maven.javadoc.plugin.version>
		<maven.source.plugin.version>3.3.1</maven.source.plugin.version>
		<maven.surefire.plugin.version>3.5.3</maven.surefire.plugin.version>
		<central.publishing.plugin.version>0.8.0</central.publishing.plugin.version>
		<codehaus.findbugs.plugin.version>3.0.5</codehaus.findbugs.plugin.version>
		<codehaus.flatten.plugin.version>1.7.1</codehaus.flatten.plugin.version>
		<graalvm.native.plugin.version>0.10.6</graalvm.native.plugin.version>
		<!-- Testing Properties -->
		<junit.jupiter.version>5.13.3</junit.jupiter.version>
		<junit.jupiter.platform.version>1.13.3</junit.jupiter.platform.version>
		<org.jmock.version>2.13.1</org.jmock.version>
		<!-- Implementation Properties -->
		<com.fasterxml.jackson.version>2.19.1</com.fasterxml.jackson.version>
		<org.apache.commons.cli.version>1.9.0</org.apache.commons.cli.version>
		<org.apache.commons.csv.version>1.14.0</org.apache.commons.csv.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>commons-cli</groupId>
			<artifactId>commons-cli</artifactId>
			<version>${org.apache.commons.cli.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-csv</artifactId>
			<version>${org.apache.commons.csv.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${com.fasterxml.jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-jdk8</artifactId>
			<version>${com.fasterxml.jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-jsr310</artifactId>
			<version>${com.fasterxml.jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit.jupiter.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-suite</artifactId>
			<version>${junit.jupiter.platform.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-launcher</artifactId>
			<version>${junit.jupiter.platform.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.jmock</groupId>
			<artifactId>jmock-junit5</artifactId>
			<version>${org.jmock.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>${maven.enforcer.plugin.version}</version>
				<executions>
					<execution>
						<id>enforce-maven</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>[${maven.min.version},)</version>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Prepare for Maven 5 -->
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>flatten-maven-plugin</artifactId>
				<version>${codehaus.flatten.plugin.version}</version>
				<configuration>
					<flattenMode>resolveCiFriendliesOnly</flattenMode>
					<!-- Updates POM used for install/publishing -->
					<updatePomFile>true</updatePomFile>
					<outputDirectory>${project.build.directory}</outputDirectory>
					<keepCommentsInPom>false</keepCommentsInPom>
					<pomElements>
						<build>flatten</build>
						<profiles>flatten</profiles>
						<pluginManagement>flatten</pluginManagement>
					</pomElements>
				</configuration>
				<executions>
					<execution>
						<id>flatten</id>
						<phase>process-resources</phase>
						<goals>
							<goal>flatten</goal>
						</goals>
					</execution>
					<execution>
						<id>flatten.clean</id>
						<phase>clean</phase>
						<goals>
							<goal>clean</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven.compiler.plugin.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven.surefire.plugin.version}</version>
				<dependencies>
					<dependency>
						<groupId>org.junit.jupiter</groupId>
						<artifactId>junit-jupiter-engine</artifactId>
						<version>${junit.jupiter.version}</version>
					</dependency>
				</dependencies>
				<configuration>
					<useModulePath>false</useModulePath>
					<systemProperties>
						<property>
							<name>java.util.logging.config.file</name>
							<value>logging.properties</value>
						</property>
					</systemProperties>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>findbugs-maven-plugin</artifactId>
				<version>${codehaus.findbugs.plugin.version}</version>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>${maven.assembly.plugin.version}</version>
				<configuration>
					<archive>
						<manifest>
							<mainClass>${mainClassName}</mainClass>
						</manifest>
					</archive>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id> <!-- this is used for inheritance merges -->
						<phase>package</phase> <!-- bind to the packaging phase -->
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>${maven.exec.plugin.version}</version>
				<executions>
					<execution>
						<id>java-agent</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>test</phase>
						<configuration>
							<executable>java</executable>
							<workingDirectory>${project.build.directory}</workingDirectory>
							<arguments>
								<argument>-classpath</argument>
								<classpath/>
								<argument>${mainClassName}</argument>
								<argument>-f</argument>
								<argument>${project.build.directory}/../ais.cases.txt</argument>
								<argument>-o</argument>
								<argument>json</argument>
							</arguments>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>release-sign-artifacts</id>
			<activation>
				<property>
					<name>performRelease</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-source-plugin</artifactId>
						<version>${maven.source.plugin.version}</version>
						<executions>
							<execution>
								<id>attach-sources</id>
								<goals>
									<goal>jar-no-fork</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-javadoc-plugin</artifactId>
						<version>${maven.javadoc.plugin.version}</version>
						<configuration>
							<encoding>${project.build.sourceEncoding}</encoding>
						</configuration>
						<executions>
							<execution>
								<id>attach-javadoc</id>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>${maven.gpg.plugin.version}</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>sonatype</id>
			<activation>
				<property>
					<name>performRelease</name>
					<value>true</value>
				</property>
			</activation>
			<distributionManagement>
				<!-- https://central.sonatype.org/publish/publish-portal-snapshots/#publishing-via-other-methods -->
				<snapshotRepository>
					<id>sonatype</id>
					<url>https://central.sonatype.com/repository/maven-snapshots</url>
				</snapshotRepository>
				<!-- https://central.sonatype.org/publish/publish-maven/#distribution-management-and-authentication -->
				<repository>
					<id>sonatype</id>
					<url>https://central.sonatype.com</url>
				</repository>
			</distributionManagement>
			<build>
				<plugins>
					<plugin>
						<groupId>org.sonatype.central</groupId>
						<artifactId>central-publishing-maven-plugin</artifactId>
						<version>${central.publishing.plugin.version}</version>
						<extensions>true</extensions>
						<configuration>
							<publishingServerId>sonatype</publishingServerId>
							<autoPublish>true</autoPublish>
							<waitUntil>published</waitUntil>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
		<!-- Github Packages Deployment -->
		<profile>
			<id>github</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<distributionManagement>
				<repository>
					<id>github</id>
					<url>https://maven.pkg.github.com/samypr100/aistool</url>
				</repository>
			</distributionManagement>
		</profile>
		<profile>
			<id>native</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.graalvm.buildtools</groupId>
						<artifactId>native-maven-plugin</artifactId>
						<version>${graalvm.native.plugin.version}</version>
						<extensions>true</extensions>
						<executions>
							<execution>
								<id>build-native</id>
								<goals>
									<goal>compile-no-fork</goal>
								</goals>
								<phase>package</phase>
							</execution>
							<execution>
								<id>test-native</id>
								<goals>
									<goal>test</goal>
								</goals>
								<phase>test</phase>
							</execution>
						</executions>
						<configuration>
							<buildArgs>
								<buildArg>-march=compatibility</buildArg>
								<buildArg>--gc=epsilon</buildArg>
								<buildArg>--strict-image-heap</buildArg>
								<buildArg>-H:+UnlockExperimentalVMOptions</buildArg>
								<!-- Called &#45;&#45;static-nolibc in v25 -->
								<buildArg>-H:+StaticExecutableWithDynamicLibC</buildArg>
							</buildArgs>
							<agent>
								<enabled>true</enabled>
							</agent>
							<imageName>aistool</imageName>
							<mainClass>${mainClassName}</mainClass>
							<fallback>false</fallback>
							<skipNativeTests>true</skipNativeTests>
							<debug>false</debug>
							<verbose>true</verbose>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>native-static</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.graalvm.buildtools</groupId>
						<artifactId>native-maven-plugin</artifactId>
						<configuration>
							<buildArgs combine.children="override">
								<buildArg>-march=compatibility</buildArg>
								<buildArg>--gc=epsilon</buildArg>
								<buildArg>--strict-image-heap</buildArg>
								<buildArg>--libc=musl</buildArg>
								<buildArg>--static</buildArg>
							</buildArgs>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
